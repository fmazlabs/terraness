variable "access_key" {
  type        = string
  default     = ""
 # description = "Name  (e.g. `app` or `cluster`)."
}

variable "secret_key" {
  type        = string
  default     = ""
 # description = "Name  (e.g. `app` or `cluster`)."
}
